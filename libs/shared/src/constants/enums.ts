export enum PROJECT_ROLES {
  OWNER = 'owner',
  DEVELOPER = 'developer',
  VIEWER = 'viewer',
}

export enum APPLICATION_ROLES {
  JIRA_ADMIN = 'JiraAdmin',
  JIRA_USER = 'JiraUser',
  JIRA_MANAGER = 'JiraManager',
}
